apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager-layer
  namespace: flux-system
spec:
  interval: 10m
  path: "./infrastructure/cert-manager" # Peker til mappen vi lager under
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system